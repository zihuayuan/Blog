  <% @post.comments.each do |comment| %>
  <%= div_for comment do %>
  <p>
    <strong>Posted created at <%= time_ago_in_words(comment.created_at) %> </strong><br />
    <%= h(comment.body) %>
  </p>
  <% end %>
  <% end %> 